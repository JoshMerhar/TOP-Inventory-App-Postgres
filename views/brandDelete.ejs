<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="/stylesheets/styles.css">
        <title>Delete Brand</title>
    </head>
    <body>
        <div class="container">
            <div class="nav-container">
                <%- include('navbar', { links: links }) %>
            </div>
            <div class="content-container">
                <h1>Are you sure you want to delete the brand <%= brand[0].brand_name %>?</h1>
                <%- include("partials/errors.ejs") %>
                <form class="delete-form" action="/brands/<%= brand[0].id %>/delete" method="POST">
                    <label for="password">Password: 
                        <input type="password" name="password" id="password" placeholder="Enter Password" required>
                    </label>
                    <button class="submit-button" type="submit">Delete Brand</button>
                    <div class="cancel-delete"><a href="/brands/<%= brand[0].id %>">Cancel</a></div>
                </form>
            </div>
        </div>
    </body>
</html>